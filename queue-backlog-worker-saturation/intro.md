You are the on-call responder for TaskFlow Demo. An alert fired because an async queue is growing and background work is delayed.

You will inspect a running training service using real terminal commands:

* `curl`
* `docker logs`
* `grep`
* `awk`
* `nano`
* `cat`

You will confirm service state, inspect queue metrics, compare enqueue and processing behavior, apply a safe training mitigation, verify whether backlog starts draining, and draft the first stakeholder update.

Focus on practical incident judgment: this is about recognizing async processing degradation, communicating current impact, and choosing the next safe action without jumping to a final root cause.

Clean-room note: TaskFlow Demo and all evidence in this lab are fictional training material.
