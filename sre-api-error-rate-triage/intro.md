You are the on-call responder for TaskFlow Demo. A 5xx error-rate alert fired for `api-service`, and your job is to make the first triage pass.

You will work with a running training API and real terminal commands:

* `curl`
* `docker logs`
* `grep`
* `awk`
* `nano`
* `cat`

Your task is to confirm service state, reproduce the degraded `task-create` workflow, inspect logs, estimate impact, classify severity, and draft the first stakeholder update.

Focus on practical incident judgment: what is known, what is still unknown, what should be communicated now, and what should be investigated next.

Clean-room note: TaskFlow Demo and all evidence in this lab are fictional training material.
